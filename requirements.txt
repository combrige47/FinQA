# ============================================================
# FinQA 项目依赖
# 更新于 2026-07-09 — 已迁移至千问 text-embedding-v4 API
# ============================================================

# ── Web 服务 ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # FastAPI 文件上传

# ── LLM / Embedding API ──
openai>=1.30.0                    # DeepSeek + DashScope 兼容接口

# ── 向量数据库 ──
pymilvus>=2.4.0

# ── 分词（替代本地 BGE-M3 tokenizer） ──
tiktoken>=0.7.0

# ── 数值计算 ──
numpy>=1.26.0

# ── 数据验证 ──
pydantic>=2.0.0

# ── MySQL ──
pymysql>=1.1.0

# ── PDF 转换 ──
opendataloader-pdf               # parse/pdf2md.py 使用
# docling>=2.0.0                   # tools/mineru_use.py 使用（可选）

# ── 股票数据获取（可选） ──
akshare>=1.14.0
pandas>=2.0.0
python-dateutil>=2.9.0

# ── 工具 ──
requests>=2.31.0
tqdm>=4.66.0

# ════════════════════════════════════════════════════════════
# 以下依赖已移除（迁移后不再需要）：
#   FlagEmbedding    — 本地 BGE-M3 模型（替换为千问 API）
#   transformers     — BGE-M3 底层框架
#   torch            — 模型推理（GPU/CPU 计算）
# ════════════════════════════════════════════════════════════
